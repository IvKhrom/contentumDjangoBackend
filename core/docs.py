# docs.py
from drf_yasg import openapi
from drf_yasg.utils import swagger_auto_schema
from rest_framework import status

swagger_description = """
Contentum — MVP backend для генерации контента через интерактивный чат-подход.

## 🎯 Основная концепция
Сотрудники регистрируются, создают чат — система задаёт вопросы по шагам, на каждый ответ пользователя система автоматически выдаёт следующий вопрос. После прохождения всех шагов собираются PromptParameters и составляется оптимизированный промпт для Kandinsky. Запускается автоматическая генерация изображения.

## 🔄 Рабочий процесс
1. **Регистрация/авторизация** пользователя
2. **Создание чата** или продолжение существующего незавершенного
3. **Диалог с системой** - последовательные вопросы о параметрах контента
4. **Автоматическая генерация** - после завершения опроса сразу запускается создание изображения
5. **Получение результата** - изображение доступно для скачивания по ссылкам

## 🚀 Ключевые особенности
- У пользователя может быть только один активный незавершенный чат одновременно
- Автоматический переход между шагами вопросов
- Интеграция с Kandinsky API для генерации изображений
- Base64 хранение сгенерированных изображений
- JWT аутентификация с ролевой моделью (ADMIN/EMPLOYEE)
- Два способа генерации: через чат и через форму

## 📊 Статистика flow
- **9 обязательных вопросов** - полный цикл сбора параметров через чат
- **Форма генерации** - все параметры сразу

## 🔧 Технические особенности
- Промпт автоматически оптимизируется под ограничения Kandinsky (макс. 800 символов)
- Поддержка форматов: 9:16, 16:9, 1:1, 4:5, 2:3
- Визуальные стили, цветовые палитры, эмоциональные тона
- Проверка качества сгенерированных фото
- Автоматическая перегенерация при проблемах с качеством
"""

# Базовые схемы ответов
success_response_schema = openapi.Schema(
    type=openapi.TYPE_OBJECT,
    properties={
        'status': openapi.Schema(type=openapi.TYPE_STRING, example='success'),
        'message': openapi.Schema(type=openapi.TYPE_STRING),
        'data': openapi.Schema(type=openapi.TYPE_OBJECT)
    }
)

error_response_schema = openapi.Schema(
    type=openapi.TYPE_OBJECT,
    properties={
        'status': openapi.Schema(type=openapi.TYPE_STRING, example='error'),
        'message': openapi.Schema(type=openapi.TYPE_STRING)
    }
)

# =============================================================================
# USER SCHEMAS
# =============================================================================

user_create_schema = swagger_auto_schema(
    operation_description="""
# 📝 Регистрация нового пользователя
    
**Роль:** PUBLIC

**Что делает этот запрос:**
- Создает нового сотрудника в системе
- Автоматически назначает роль EMPLOYEE
- Проверяет уникальность email и валидность домена
- Хэширует пароль для безопасного хранения

**Поддерживаемые домены email:**
- gmail.com, yandex.ru, mail.ru, sberbank.ru

**Требования к паролю:**
- Минимум 8 символов
- Совпадение password и passwordConfirm

**Response:**
- 201: ✅ Пользователь создан, можно сразу авторизоваться
- 400: ❌ Ошибка валидации (email занят, домен не поддерживается, пароли не совпадают)
""",
    request_body=openapi.Schema(
        type=openapi.TYPE_OBJECT,
        required=['email', 'fullName', 'password', 'passwordConfirm'],
        properties={
            'email': openapi.Schema(type=openapi.TYPE_STRING, description='Email пользователя', example='ivan@example.com'),
            'fullName': openapi.Schema(type=openapi.TYPE_STRING, description='Полное имя', example='Иван Иванов'),
            'password': openapi.Schema(type=openapi.TYPE_STRING, description='Пароль (мин. 8 символов)', example='securePassword123', min_length=8),
            'passwordConfirm': openapi.Schema(type=openapi.TYPE_STRING, description='Подтверждение пароля', example='securePassword123'),
        }
    ),
    responses={
        status.HTTP_201_CREATED: openapi.Response('✅ Пользователь успешно зарегистрирован', success_response_schema),
        status.HTTP_400_BAD_REQUEST: openapi.Response('❌ Ошибка валидации', error_response_schema)
    }
)

user_login_schema = swagger_auto_schema(
    operation_description="""
# 🔐 Вход в систему
    
**Роль:** PUBLIC

**Что делает этот запрос:**
- Проверяет email и пароль пользователя
- Генерирует JWT access и refresh токены
- Возвращает информацию о пользователе вместе с токенами

**Срок действия токенов:**
- Access токен: 60 минут
- Refresh токен: 7 дней

**Использование токенов:**
- Для защищенных endpoint'ов: `Authorization: Bearer <access_token>`
- Для обновления: POST /api/auth/refresh/ с refresh_token

**Response:**
- 200: ✅ Успешная аутентификация + токены + данные пользователя
- 401: ❌ Неверные учетные данные
""",
    request_body=openapi.Schema(
        type=openapi.TYPE_OBJECT,
        required=['email', 'password'],
        properties={
            'email': openapi.Schema(type=openapi.TYPE_STRING, example='ivan@example.com'),
            'password': openapi.Schema(type=openapi.TYPE_STRING, example='securePassword123'),
        }
    ),
    responses={
        status.HTTP_200_OK: openapi.Response('✅ Успешная аутентификация', openapi.Schema(
            type=openapi.TYPE_OBJECT,
            properties={
                'access': openapi.Schema(type=openapi.TYPE_STRING, description='JWT access токен'),
                'refresh': openapi.Schema(type=openapi.TYPE_STRING, description='JWT refresh токен'),
                'user': openapi.Schema(type=openapi.TYPE_OBJECT, description='Информация о пользователе')
            }
        )),
        status.HTTP_401_UNAUTHORIZED: openapi.Response('❌ Неверные учетные данные', error_response_schema)
    }
)

user_me_schema = swagger_auto_schema(
    operation_description="""
# 👤 Получение информации о текущем пользователе
    
**Роль:** AUTHENTICATED

**Что делает этот запрос:**
- Возвращает полную информацию о авторизованном пользователе
- Показывает email, имя, роль и статус аккаунта

**Использование:**
- Для отображения профиля пользователя в интерфейсе
- Для проверки роли и прав доступа
- При инициализации сессии пользователя

**Требования:**
- Валидный JWT токен в заголовке `Authorization: Bearer <token>`

**Response:**
- 200: ✅ Данные пользователя
- 401: ❌ Требуется аутентификация
""",
    responses={
        status.HTTP_200_OK: openapi.Response('✅ Успешно', success_response_schema),
        status.HTTP_401_UNAUTHORIZED: openapi.Response('❌ Требуется аутентификация', error_response_schema)
    }
)

user_summary_schema = swagger_auto_schema(
    operation_description="""
# 📊 Получение сводки по чатам пользователя
    
**Роль:** AUTHENTICATED

**Что делает этот запрос:**
- Показывает статистику по чатам пользователя
- Считает общее количество чатов, завершенных и активных
- Отображает общее количество сообщений

**Возвращаемые метрики:**
- `total_chats` - всего чатов пользователя
- `completed_chats` - завершенных чатов (все вопросы пройдены)  
- `active_chats` - активных чатов (опрос не завершен)
- `total_messages` - всего сообщений во всех чатах

**Использование:**
- Для дашборда пользователя
- Для отображения прогресса
- При анализе активности

**Response:**
- 200: ✅ Статистика пользователя
- 401: ❌ Требуется аутентификация
""",
    responses={
        status.HTTP_200_OK: openapi.Response('✅ Успешно', success_response_schema),
        status.HTTP_401_UNAUTHORIZED: openapi.Response('❌ Требуется аутентификация', error_response_schema)
    }
)

# =============================================================================
# CHAT SCHEMAS
# =============================================================================

chat_list_schema = swagger_auto_schema(
    operation_description="""
# 💬 Получение списка чатов пользователя
    
**Роль:** AUTHENTICATED

**Что делает этот запрос:**
- Возвращает все чаты текущего пользователя
- Для администраторов показывает чаты всех пользователей
- Включает мета-информацию: количество сообщений, последнее сообщение

**Доступные фильтры:**
- `isActive` - фильтр по активности чата
- `createdAt`, `updatedAt` - фильтрация по датам
- Поиск по названию чата, email и имени пользователя
- Сортировка по дате создания, обновления, названию

**Поля ответа:**
- Основная информация о чате + `messages`, `messageCount`, `lastMessage`
- `flow_step` - текущий шаг в опросе (0-9)

**Response:**
- 200: ✅ Список чатов с пагинацией
- 401: ❌ Требуется аутентификация
""",
    responses={
        status.HTTP_200_OK: openapi.Response('✅ Успешно', success_response_schema),
        status.HTTP_401_UNAUTHORIZED: openapi.Response('❌ Требуется аутентификация', error_response_schema)
    }
)

chat_create_schema = swagger_auto_schema(
    operation_description="""
# 🆕 Создание нового чата
    
**Роль:** AUTHENTICATED

**Что делает этот запрос:**
- Создает новый чат для пользователя
- Автоматически задает первый вопрос системы
- Проверяет, что у пользователя нет других незавершенных чатов

**❗ Важные ограничения:**
- У пользователя может быть только один незавершенный чат одновременно
- Перед созданием нового нужно завершить текущий или удалить его

**Автоматические действия:**
- Если title не указан - генерируется "Чат №X"
- Сразу создается первый SYSTEM вопрос: "Кратко опишите идею или цель контента"
- Если передан initialMessage - создается USER сообщение

**Response:**
- 201: ✅ Чат создан + первый системный вопрос + данные чата
- 400: ❌ Ошибка / есть незавершённый чат / ошибка валидации
""",
    request_body=openapi.Schema(
        type=openapi.TYPE_OBJECT,
        properties={
            'title': openapi.Schema(type=openapi.TYPE_STRING, description='Название чата (опционально)', example='Мой контент для Instagram'),
            'initialMessage': openapi.Schema(type=openapi.TYPE_STRING, description='Первое пользовательское сообщение (опционально)', example='Хочу создать фото для поста'),
        }
    ),
    responses={
        status.HTTP_201_CREATED: openapi.Response('✅ Чат создан и задан первый вопрос', success_response_schema),
        status.HTTP_400_BAD_REQUEST: openapi.Response('❌ Ошибка / есть незавершённый чат', error_response_schema)
    }
)

chat_empty_schema = swagger_auto_schema(
    operation_description="""
# 🔍 Проверка пустого чата
    
**Роль:** AUTHENTICATED

**Что делает этот запрос:**
- Проверяет, есть ли у пользователя чат без пользовательских сообщений
- Возвращает данные чата, если он найден
- Помогает определить, можно ли создавать новый чат

**Что такое "пустой чат":**
- Чат, в котором есть только системные сообщения (вопросы)
- Нет ни одного сообщения от пользователя (USER тип)

**Использование в workflow:**
1. При загрузке приложения - проверка пустого чата
2. Если есть - предложить продолжить
3. Если нет - разрешить создание нового

**Response:**
- 200: ✅ Найден чат без пользовательских сообщений + данные чата
- 200: ✅ Нет чатов без пользовательских сообщений + data: null
""",
    responses={
        status.HTTP_200_OK: openapi.Response('✅ Результат проверки', openapi.Schema(
            type=openapi.TYPE_OBJECT,
            properties={
                'status': openapi.Schema(type=openapi.TYPE_STRING),
                'message': openapi.Schema(type=openapi.TYPE_STRING),
                'data': openapi.Schema(
                    type=openapi.TYPE_OBJECT,
                    nullable=True,
                    description='Данные чата если есть не пустой, иначе null'
                )
            }
        ))
    }
)

chat_messages_schema = swagger_auto_schema(
    operation_description="""
# 💭 Получение сообщений чата
    
**Роль:** AUTHENTICATED (только свои чаты, ADMIN - все)

**Что делает этот запрос:**
- Возвращает все сообщения указанного чата с пагинацией
- Показывает полную историю переписки с системой
- Включает как пользовательские, так и системные сообщения

**Структура сообщений:**
- `USER` - ответы пользователя на вопросы системы
- `SYSTEM` - вопросы системы согласно flow
- Сообщения отсортированы по времени создания (хронологический порядок)

**Flow вопросов (9 шагов):**
1. idea - идея контента
2. event_name - название постановки
3. event_genre - жанр события
4. visual_style - художественный стиль
5. composition_focus - акцент композиции
6. color_palette - цветовая палитра
7. visual_associations - слова-ассоциации
8. platform - платформа публикации
9. aspect_ratio - формат кадра

**Response:**
- 200: ✅ Список сообщений с пагинацией
- 404: ❌ Чат не найден / нет доступа
""",
    responses={
        status.HTTP_200_OK: openapi.Response('✅ Успешно', success_response_schema),
        status.HTTP_404_NOT_FOUND: openapi.Response('❌ Чат не найден', error_response_schema)
    }
)

chat_generation_status_schema = swagger_auto_schema(
    operation_description="""
# 📊 Проверка статуса генерации для чата
    
**Роль:** AUTHENTICATED

**Что делает этот запрос:**
- Возвращает статус последней задачи генерации для указанного чата
- Показывает прогресс генерации изображения
- Информирует о наличии готового изображения

**Возможные статусы:**
- `PENDING` - задача создана, ожидает обработки
- `RUNNING` - генерация выполняется
- `SUCCESS` - успешно завершено, изображение готово
- `FAILED` - ошибка генерации

**Поля ответа:**
- `generation_status` - текущий статус
- `task_id` - ID задачи генерации
- `has_image` - есть ли готовое изображение
- `last_error` - текст ошибки если статус FAILED
- `created_at`, `updated_at` - временные метки

**Response:**
- 200: ✅ Статус генерации
- 404: ❌ Чат не найден
""",
    responses={
        status.HTTP_200_OK: openapi.Response('✅ Статус генерации', success_response_schema),
        status.HTTP_404_NOT_FOUND: openapi.Response('❌ Чат не найден', error_response_schema)
    }
)

chat_generated_images_schema = swagger_auto_schema(
    operation_description="""
# 🖼️ Получение всех сгенерированных изображений для чата
    
**Роль:** AUTHENTICATED

**Что делает этот запрос:**
- Возвращает список всех успешно сгенерированных изображений для чата
- Предоставляет URLs для просмотра и скачивания
- Показывает историю генераций с промптами и датами

**Формат данных изображения:**
- Изображения хранятся в формате Base64
- Доступны для скачивания как PNG файлы
- Поддерживаются прямые ссылки для интеграции

**Поля каждого изображения:**
- `task_id` - ID задачи генерации
- `prompt` - промпт использованный для генерации
- `created_at` - дата создания
- `image_url` - URL для просмотра изображения
- `download_url` - URL для скачивания файла

**Response:**
- 200: ✅ Список сгенерированных изображений
- 404: ❌ Чат не найден
""",
    responses={
        status.HTTP_200_OK: openapi.Response('✅ Список изображений', success_response_schema),
        status.HTTP_404_NOT_FOUND: openapi.Response('❌ Чат не найден', error_response_schema)
    }
)

# =============================================================================
# MESSAGE SCHEMAS
# =============================================================================

message_create_schema = swagger_auto_schema(
    operation_description="""
# ✉️ Отправка сообщения в чат
    
**Роль:** AUTHENTICATED

**Что делает этот запрос:**
- Отправляет сообщение от пользователя в указанный чат
- **Автоматически запускает обработку flow опроса**
- Возвращает следующий вопрос системы или завершает сбор параметров

**🎯 Ключевая логика:**
- На USER сообщение система автоматически отвечает следующим вопросом
- Всего 9 шагов вопросов о параметрах контента
- После 9-го ответа запускается автоматическая генерация изображения

**📝 Процесс завершения flow:**
1. Собираются все ответы в PromptParameters
2. Создается оптимизированный промпт для Kandinsky
3. Запускается задача генерации изображения с проверкой качества
4. Возвращаются IDs созданных объектов + ссылки на изображение

**🔄 Автоматическая перегенерация:**
- Если сгенерированное фото не проходит проверку качества
- Система автоматически исправляет промпт и генерирует заново
- До 3 попыток перегенерации
- Возвращает количество попыток и исправленные проблемы

**📝 Новый формат сообщений:**
    - Поле `content` всегда содержит JSON объект с двумя полями:
      - `type`: тип сообщения ("text" или "image")
      - `info`: содержимое в зависимости от типа:
        - Для `type="text"`: строка с текстом сообщения
        - Для `type="image"`: объект с данными изображения
    
    **📸 Формат сообщения с изображением:**
    ```json
    {
      "content": {
        "type": "image",
        "info": {
          "task_id": "uuid задачи генерации",
          "prompt": "промпт использованный для генерации",
          "image_url": "URL для просмотра изображения",
          "download_url": "URL для скачивания файла",
          "regeneration_attempts": 2,
          "total_attempts": 3
        }
      }
    }
    ```
    
    **📝 Формат текстового сообщения:**
    ```json
    {
      "content": {
        "type": "text",
        "info": "Текст сообщения"
      }
    }
    ```
    
    **📤 Пример отправки пользовательского сообщения:**
    ```json
    {
      "chat": "uuid чата",
      "content": {
        "type": "text",
        "info": "Мой ответ на вопрос"
      },
      "messageType": "USER"
    }
    ```



**Response:**
- 201: ✅ Сообщение принято + следующий SYSTEM вопрос
- 201: ✅ Flow завершён + запущена генерация + данные промптов
- 400: ❌ Ошибка валидации
""",
    request_body=openapi.Schema(
        type=openapi.TYPE_OBJECT,
        required=['chat', 'content'],
        properties={
            'chat': openapi.Schema(type=openapi.TYPE_STRING, description='UUID чата', example='123e4567-e89b-12d3-a456-426614174000'),
            'content': openapi.Schema(type=openapi.TYPE_STRING, description='Текст сообщения', example='Фото для Instagram'),
            'messageType': openapi.Schema(type=openapi.TYPE_STRING, description='USER or SYSTEM (по умолчанию USER)', example='USER', enum=['USER', 'SYSTEM']),
        }
    ),
    responses={
        status.HTTP_201_CREATED: openapi.Response('✅ Сообщение принято, возможно возвращён system_message', openapi.Schema(
            type=openapi.TYPE_OBJECT,
            properties={
                'status': openapi.Schema(type=openapi.TYPE_STRING),
                'message': openapi.Schema(type=openapi.TYPE_STRING),
                'data': openapi.Schema(
                    type=openapi.TYPE_OBJECT,
                    properties={
                        'user_message': openapi.Schema(type=openapi.TYPE_OBJECT, description='Отправленное сообщение пользователя'),
                        'system_message': openapi.Schema(type=openapi.TYPE_OBJECT, description='Следующий вопрос системы (если есть)'),
                        'prompt_parameters_id': openapi.Schema(type=openapi.TYPE_STRING, description='ID параметров при завершении flow'),
                        'prompt_history_id': openapi.Schema(type=openapi.TYPE_STRING, description='ID истории промпта при завершении'),
                        'assembled_prompt': openapi.Schema(type=openapi.TYPE_STRING, description='Собранный промпт при завершении'),
                        'generation_task_id': openapi.Schema(type=openapi.TYPE_STRING, description='ID задачи генерации при завершении'),
                        'image_url': openapi.Schema(type=openapi.TYPE_STRING, description='URL для получения изображения при завершении'),
                        'regeneration_attempts': openapi.Schema(type=openapi.TYPE_INTEGER, description='Количество перегенераций'),
                        'total_attempts': openapi.Schema(type=openapi.TYPE_INTEGER, description='Общее количество попыток генерации'),
                    }
                )
            }
        )),
        status.HTTP_400_BAD_REQUEST: openapi.Response('❌ Ошибка валидации', error_response_schema)
    }
)

# =============================================================================
# GENERATION TASKS SCHEMAS
# =============================================================================

generation_task_image_schema = swagger_auto_schema(
    operation_description="""
# 🖼️ Получение сгенерированного изображения (JSON с Base64)
    
**Роль:** AUTHENTICATED (для своих изображений), PUBLIC (для скачивания)

**Что делает этот запрос:**
- Возвращает сгенерированное изображение в формате Base64 в JSON
- Предоставляет мета-информацию о задаче генерации
- **Доступен без авторизации** по прямой ссылке

**Поля JSON ответа:**
- `image_base64` - изображение в формате Base64
- `task_id` - ID задачи генерации
- `prompt` - промпт использованный для генерации
- `created_at` - дата создания
- `download_url` - прямая ссылка для скачивания файла
- `preview_url` - ссылка для просмотра в браузере

**Публичный доступ:**
- Изображения доступны по прямым ссылкам без токена
- Для просмотра в браузере: `/api/generation-tasks/{id}/image-file/`
- Для скачивания: `/api/generation-tasks/{id}/download/`

**Response:**
- 200: ✅ Изображение в Base64 + мета-данные
- 404: ❌ Изображение не найдено / задача не завершена
""",
    manual_parameters=[
        openapi.Parameter('format', openapi.IN_QUERY, description="Формат ответа: 'json' или 'file'", type=openapi.TYPE_STRING, enum=['json', 'file'], default='json')
    ],
    responses={
        status.HTTP_200_OK: openapi.Response('✅ Изображение', openapi.Schema(
            type=openapi.TYPE_OBJECT,
            properties={
                'status': openapi.Schema(type=openapi.TYPE_STRING),
                'data': openapi.Schema(
                    type=openapi.TYPE_OBJECT,
                    properties={
                        'image_base64': openapi.Schema(type=openapi.TYPE_STRING, description='Изображение в формате Base64'),
                        'task_id': openapi.Schema(type=openapi.TYPE_STRING, description='ID задачи генерации'),
                        'prompt': openapi.Schema(type=openapi.TYPE_STRING, description='Промпт использованный для генерации'),
                        'created_at': openapi.Schema(type=openapi.TYPE_STRING, description='Дата создания'),
                        'download_url': openapi.Schema(type=openapi.TYPE_STRING, description='URL для скачивания файла'),
                        'preview_url': openapi.Schema(type=openapi.TYPE_STRING, description='URL для просмотра в браузере'),
                    }
                )
            }
        )),
        status.HTTP_404_NOT_FOUND: openapi.Response('❌ Изображение не найдено', error_response_schema)
    }
)

generation_task_download_schema = swagger_auto_schema(
    operation_description="""
# 📥 Скачивание изображения как файла
    
**Роль:** AUTHENTICATED (для своих изображений), PUBLIC (для скачивания)

**Что делает этот запрос:**
- Возвращает сгенерированное изображение как PNG файл для скачивания
- Устанавливает правильные HTTP заголовки для загрузки файла
- Автоматическое имя файла: `generated_image_{task_id}.png`
- **Доступен без авторизации** по прямой ссылке

**Особенности:**
- Прямое скачивание без промежуточного Base64
- Поддержка браузерного диалога "Сохранить как..."
- Интеграция с HTML тегом `<a download>`

**Публичный доступ:**
- Изображения доступны для скачивания по прямой ссылке
- Не требуется JWT токен или аутентификация
- Защита только через уникальность UUID задачи

**Response:**
- 200: ✅ PNG файл с правильными заголовками
- 404: ❌ Изображение не найдено
- 500: ❌ Ошибка декодирования изображения
""",
    responses={
        status.HTTP_200_OK: openapi.Response('✅ PNG файл', openapi.Schema(type=openapi.TYPE_FILE)),
        status.HTTP_404_NOT_FOUND: openapi.Response('❌ Изображение не найдено', error_response_schema),
        status.HTTP_500_INTERNAL_SERVER_ERROR: openapi.Response('❌ Ошибка декодирования', error_response_schema)
    }
)

# =============================================================================
# PROMPT SCHEMAS
# =============================================================================

prompt_assemble_schema = swagger_auto_schema(
    operation_description="""
# 🧩 Сборка промпта из параметров
    
**Роль:** AUTHENTICATED

**Что делает этот запрос:**
- Берет параметры контента (вручную или из сохраненного набора)
- Подставляет параметры в шаблон промпта
- Сохраняет историю сборки и возвращает готовый промпт

**Источники параметров:**
- 📂 `prompt_parameters_id` - использовать сохраненные параметры из чата
- ✏️ `parameters` - передать параметры вручную в JSON
- 🎯 `template_id` - указать конкретный шаблон (или используется активный)

**Автоматическая логика:**
- Если не указан template_id - используется активный шаблон
- Создается запись в PromptHistory для аудита
- Возвращается собранный промпт для немедленного использования
""",
    request_body=openapi.Schema(
        type=openapi.TYPE_OBJECT,
        properties={
            'prompt_parameters_id': openapi.Schema(type=openapi.TYPE_STRING, description='ID сохранённых параметров', example='123e4567-e89b-12d3-a456-426614174000'),
            'parameters': openapi.Schema(
                type=openapi.TYPE_OBJECT, 
                description='Параметры промпта в формате JSON',
                example={
                    'idea': 'вдохновляющее изображение о театре',
                    'visual_style': 'кинематографичный',
                    'platform': 'Instagram',
                    'aspect_ratio': '1:1',
                    'composition_focus': 'архитектура',
                    'color_palette': 'тёплые тона'
                }
            ),
            'template_id': openapi.Schema(type=openapi.TYPE_STRING, description='ID шаблона промпта (опционально)', example='123e4567-e89b-12d3-a456-426614174000'),
        }
    ),
    responses={
        status.HTTP_200_OK: openapi.Response('✅ Промпт собран', openapi.Schema(
            type=openapi.TYPE_OBJECT,
            properties={
                'status': openapi.Schema(type=openapi.TYPE_STRING),
                'message': openapi.Schema(type=openapi.TYPE_STRING),
                'data': openapi.Schema(
                    type=openapi.TYPE_OBJECT,
                    properties={
                        'assembled_prompt': openapi.Schema(type=openapi.TYPE_STRING, description='Готовый промпт для генерации'),
                        'history_id': openapi.Schema(type=openapi.TYPE_STRING, description='ID записи в истории промптов')
                    }
                )
            }
        )),
        status.HTTP_400_BAD_REQUEST: openapi.Response('❌ Ошибка / нет активного шаблона', error_response_schema)
    }
)

prompt_generate_schema = swagger_auto_schema(
    operation_description="""
# 🎨 Генерация медиа контента (ручной запуск)
    
**Роль:** AUTHENTICATED

**Что делает этот запрос:**
- Собирает промпт из параметров (если не передан готовый)
- Создает задачу генерации в системе
- Выполняет multiple attempts с quality check
- Возвращает URL сгенерированного контента

**🎯 Отличие от автоматической генерации:**
- Ручной запуск вне контекста чата
- Возможность тонкой настройки параметров
- Контроль над количеством попыток генерации

**Процесс генерации:**
1. 🔄 Сборка промпта из параметров
2. 🚀 Отправка в Kandinsky API
3. ✅ Проверка качества сгенерированного контента
4. 🔁 При провале - перефразирование и повтор (до max_attempts)
5. 📦 Возврат результата или ошибки после всех попыток
""",
    request_body=openapi.Schema(
        type=openapi.TYPE_OBJECT,
        properties={
            'prompt_parameters_id': openapi.Schema(type=openapi.TYPE_STRING, description='ID сохранённых параметров', example='123e4567-e89b-12d3-a456-426614174000'),
            'parameters': openapi.Schema(
                type=openapi.TYPE_OBJECT, 
                description='Параметры промпта в формате JSON',
                example={
                    'idea': 'вдохновляющее изображение о театре',
                    'visual_style': 'кинематографичный',
                    'platform': 'Instagram',
                    'aspect_ratio': '1:1',
                    'composition_focus': 'архитектура',
                    'color_palette': 'тёплые тона',
                }
            ),
            'template_id': openapi.Schema(type=openapi.TYPE_STRING, description='ID шаблона промпта (опционально)', example='123e4567-e89b-12d3-a456-426614174000'),
            'max_attempts': openapi.Schema(type=openapi.TYPE_INTEGER, description='Число попыток перегенерации при провале quality-check', default=3, example=3),
        }
    ),
    responses={
        status.HTTP_200_OK: openapi.Response('✅ Генерация завершена', openapi.Schema(
            type=openapi.TYPE_OBJECT,
            properties={
                'status': openapi.Schema(type=openapi.TYPE_STRING),
                'message': openapi.Schema(type=openapi.TYPE_STRING),
                'data': openapi.Schema(
                    type=openapi.TYPE_OBJECT,
                    properties={
                        'task_id': openapi.Schema(type=openapi.TYPE_STRING, description='ID задачи генерации'),
                        'result_url': openapi.Schema(type=openapi.TYPE_STRING, description='URL сгенерированного контента')
                    }
                )
            }
        )),
        status.HTTP_500_INTERNAL_SERVER_ERROR: openapi.Response('❌ Не удалось получить корректный результат после попыток', error_response_schema)
    }
)

prompt_template_list_schema = swagger_auto_schema(
    operation_description="""
# 📋 Получение списка шаблонов промптов
    
**Роль:** AUTHENTICATED (только просмотр), ADMIN (полный доступ)

**Что делает этот запрос:**
- Возвращает все доступные шаблоны промптов
- Показывает активные и неактивные шаблоны
- Для администраторов - полный доступ к CRUD

**Права доступа:**
- Все пользователи: GET (просмотр)
- Только ADMIN: POST, PUT, PATCH, DELETE (управление)
""",
    responses={
        status.HTTP_200_OK: openapi.Response('✅ Успешно', success_response_schema),
        status.HTTP_401_UNAUTHORIZED: openapi.Response('❌ Требуется аутентификация', error_response_schema),
        status.HTTP_403_FORBIDDEN: openapi.Response('❌ Недостаточно прав', error_response_schema)
    }
)

prompt_parameters_list_schema = swagger_auto_schema(
    operation_description="""
# 📊 Получение списка параметров промптов
    
**Роль:** AUTHENTICATED (только свои), ADMIN (все)

**Что делает этот запрос:**
- Возвращает историю параметров пользователя
- Показывает все сохраненные наборы параметров из чатов
- Для администраторов - параметры всех пользователей

**Структура параметров:**
- 9 полей соответствующих вопросам flow
- Обязательные: idea, visual_style, composition_focus, platform, aspect_ratio
- Опциональные: event_name, event_genre, color_palette, visual_associations
""",
    responses={
        status.HTTP_200_OK: openapi.Response('✅ Успешно', success_response_schema),
        status.HTTP_401_UNAUTHORIZED: openapi.Response('❌ Требуется аутентификация', error_response_schema)
    }
)

# =============================================================================
# FORM GENERATION SCHEMAS (NEW)
# =============================================================================

form_generation_schema = swagger_auto_schema(
    operation_description="""
# 📝 Генерация через форму (все параметры сразу)

**Роль:** AUTHENTICATED

**Что делает этот запрос:**
- Принимает все 9 параметров контента сразу через форму
- Обогащает короткие тексты через GigaChat
- Собирает промпт и оптимизирует его для Kandinsky
- Генерирует изображение с проверкой качества
- При необходимости автоматически перегенерирует
- Возвращает готовое изображение или статус

**🔄 Процесс генерации:**
1. ✅ Валидация и обогащение параметров
2. 🧩 Сборка промпта из шаблона
3. 🎨 Генерация через Kandinsky API
4. 👁️ Проверка качества (руки, лица, анатомия) если включено
5. 🔁 При неудаче - исправление промпта и повтор (до max_regeneration_attempts раз)
6. 📦 Возврат результата

**📊 Пример времени:**
- Без проверки: ~30-45 секунд
- С проверкой: ~45-90 секунд (в зависимости от попыток)

**🎯 Ключевые параметры:**
- `enable_photo_check` - включить/выключить проверку качества
- `max_regeneration_attempts` - максимальное количество перегенераций (1-10)
- `aspect_ratio` - доступные форматы: 9:16, 16:9, 1:1, 4:5, 2:3

**Response:**
- 201: ✅ Генерация запущена + данные задачи
- 400: ❌ Ошибка валидации параметров
""",
    request_body=openapi.Schema(
        type=openapi.TYPE_OBJECT,
        required=['idea', 'visual_style', 'composition_focus', 'color_palette', 'visual_associations', 'platform', 'aspect_ratio'],
        properties={
            'idea': openapi.Schema(type=openapi.TYPE_STRING, description='Идея контента (например: "Концертный зал на постановке")', example='Театральная сцена с ярким освещением'),
            'event_name': openapi.Schema(type=openapi.TYPE_STRING, description='Название постановки (опционально)', example='Щелкунчик'),
            'event_genre': openapi.Schema(type=openapi.TYPE_STRING, description='Жанр события (опционально)', example='балет'),
            'visual_style': openapi.Schema(type=openapi.TYPE_STRING, description='Художественный стиль', example='неон'),
            'composition_focus': openapi.Schema(type=openapi.TYPE_STRING, description='Фокус композиции', example='танцоры на сцене'),
            'color_palette': openapi.Schema(type=openapi.TYPE_STRING, description='Цветовая палитра', example='тёплая с акцентами золотого'),
            'visual_associations': openapi.Schema(type=openapi.TYPE_STRING, description='Слова-ассоциации', example='огни сцены, движение, свет прожекторов'),
            'platform': openapi.Schema(type=openapi.TYPE_STRING, description='Платформа публикации', example='Instagram'),
            'aspect_ratio': openapi.Schema(type=openapi.TYPE_STRING, description='Формат кадра', example='9:16', enum=['9:16', '16:9', '1:1', '4:5', '2:3']),
            'enable_photo_check': openapi.Schema(type=openapi.TYPE_BOOLEAN, description='Включить проверку качества фото', default=True),
            'max_regeneration_attempts': openapi.Schema(type=openapi.TYPE_INTEGER, description='Макс. количество перегенераций при провале проверки', default=3, minimum=1, maximum=10),
        }
    ),
    responses={
        status.HTTP_201_CREATED: openapi.Response('✅ Генерация запущена', openapi.Schema(
            type=openapi.TYPE_OBJECT,
            properties={
                'status': openapi.Schema(type=openapi.TYPE_STRING),
                'message': openapi.Schema(type=openapi.TYPE_STRING),
                'data': openapi.Schema(
                    type=openapi.TYPE_OBJECT,
                    properties={
                        'task_id': openapi.Schema(type=openapi.TYPE_STRING, description='ID задачи генерации'),
                        'status': openapi.Schema(type=openapi.TYPE_STRING, description='Текущий статус (PENDING/RUNNING/SUCCESS/FAILED)'),
                        'assembled_prompt': openapi.Schema(type=openapi.TYPE_STRING, description='Собранный промпт'),
                        'image_url': openapi.Schema(type=openapi.TYPE_STRING, description='URL для просмотра изображения (если готово)'),
                        'download_url': openapi.Schema(type=openapi.TYPE_STRING, description='URL для скачивания (если готово)'),
                        'generation_attempts': openapi.Schema(type=openapi.TYPE_INTEGER, description='Всего попыток генерации'),
                        'regeneration_attempts': openapi.Schema(type=openapi.TYPE_INTEGER, description='Количество перегенераций'),
                        'problems_fixed': openapi.Schema(type=openapi.TYPE_ARRAY, items=openapi.Schema(type=openapi.TYPE_STRING), description='Исправленные проблемы'),
                        'estimated_time_seconds': openapi.Schema(type=openapi.TYPE_INTEGER, description='Примерное время генерации'),
                    }
                )
            }
        )),
        status.HTTP_400_BAD_REQUEST: openapi.Response('❌ Ошибка валидации', error_response_schema)
    }
)

form_generation_aspect_ratios_schema = swagger_auto_schema(
    operation_description="""
# 📐 Получение доступных форматов изображений
    
**Роль:** AUTHENTICATED

**Что делает этот запрос:**
- Возвращает список всех доступных форматов изображений
- Показывает размеры в пикселях для каждого формата
- Описывает типичное использование каждого формата

**Доступные форматы:**
- **9:16** - Вертикальный для Instagram Stories, TikTok
- **16:9** - Горизонтальный для YouTube, десктоп
- **1:1** - Квадрат для Instagram постов, аватарок
- **4:5** - Портрет для Facebook/Instagram
- **2:3** - Портрет для печати, постеров

**Использование:**
- Для отображения выпадающего списка в UI формы
- Для информации о рекомендуемых размерах
""",
    responses={
        status.HTTP_200_OK: openapi.Response('✅ Список форматов', openapi.Schema(
            type=openapi.TYPE_OBJECT,
            properties={
                'status': openapi.Schema(type=openapi.TYPE_STRING),
                'data': openapi.Schema(
                    type=openapi.TYPE_ARRAY,
                    items=openapi.Schema(
                        type=openapi.TYPE_OBJECT,
                        properties={
                            'value': openapi.Schema(type=openapi.TYPE_STRING, description='Значение для отправки в API'),
                            'label': openapi.Schema(type=openapi.TYPE_STRING, description='Человекочитаемое описание'),
                            'width': openapi.Schema(type=openapi.TYPE_INTEGER, description='Ширина в пикселях'),
                            'height': openapi.Schema(type=openapi.TYPE_INTEGER, description='Высота в пикселях'),
                        }
                    )
                )
            }
        ))
    }
)

form_generation_visual_styles_schema = swagger_auto_schema(
    operation_description="""
# 🎨 Получение популярных визуальных стилей
    
**Роль:** AUTHENTICATED

**Что делает этот запрос:**
- Возвращает список популярных визуальных стилей
- Показывает стили, которые хорошо работают с Kandinsky API
- Можно использовать для автодополнения или выпадающего списка

**Примеры стилей:**
- реализм, минимализм, арт-деко, неон, сюрреализм
- кинематографичный, живописный, цифровой арт, граффити
- аниме, пиксель арт, 3D рендер, акварель, масляная живопись

**Использование:**
- Для автокомплита при вводе стиля в форме
- Для отображения рекомендуемых вариантов
""",
    responses={
        status.HTTP_200_OK: openapi.Response('✅ Список стилей', openapi.Schema(
            type=openapi.TYPE_OBJECT,
            properties={
                'status': openapi.Schema(type=openapi.TYPE_STRING),
                'data': openapi.Schema(
                    type=openapi.TYPE_ARRAY,
                    items=openapi.Schema(type=openapi.TYPE_STRING)
                )
            }
        ))
    }
)

# =============================================================================
# MEDIA GENERATION TASK SCHEMAS (ADDITIONAL)
# =============================================================================

generation_task_image_file_schema = swagger_auto_schema(
    operation_description="""
# 👁️ Получение изображения как файла для просмотра в браузере
    
**Роль:** AUTHENTICATED (для своих изображений), PUBLIC (для просмотра)

**Что делает этот запрос:**
- Возвращает сгенерированное изображение как PNG файл для встраивания в HTML
- Устанавливает заголовок `Content-Disposition: inline` для показа в браузере
- Автоматическое имя файла: `generated_image_{task_id}.png`
- **Доступен без авторизации** по прямой ссылке

**Отличие от скачивания:**
- Этот endpoint предназначен для **показа** изображения в браузере
- Использует `Content-Disposition: inline` вместо `attachment`
- Изображение открывается в браузере, а не скачивается

**Использование в HTML:**
```html
<img src="/api/generation-tasks/{id}/image-file/" alt="Сгенерированное изображение">
```

**Response:**
- 200: ✅ PNG файл для показа в браузере
- 404: ❌ Изображение не найдено
- 500: ❌ Ошибка декодирования
""",
    responses={
        status.HTTP_200_OK: openapi.Response('✅ PNG файл', openapi.Schema(type=openapi.TYPE_FILE)),
        status.HTTP_404_NOT_FOUND: openapi.Response('❌ Изображение не найдено', error_response_schema),
        status.HTTP_500_INTERNAL_SERVER_ERROR: openapi.Response('❌ Ошибка декодирования', error_response_schema)
    }
)

#=============================================================================
#EXPORT ALL SCHEMAS
#=============================================================================
all = [
    # User schemas
    'user_create_schema',
    'user_login_schema',
    'user_me_schema',
    'user_summary_schema',
    # Chat schemas
    'chat_list_schema',
    'chat_create_schema',
    'chat_empty_schema',
    'chat_messages_schema',
    'chat_generation_status_schema',
    'chat_generated_images_schema',

    # Message schemas
    'message_create_schema',

    # Prompt schemas
    'prompt_assemble_schema',
    'prompt_generate_schema',
    'prompt_template_list_schema',
    'prompt_parameters_list_schema',

    # Generation task schemas
    'generation_task_image_schema',
    'generation_task_image_file_schema',
    'generation_task_download_schema',

    # Form generation schemas (NEW)
    'form_generation_schema',
    'form_generation_aspect_ratios_schema',
    'form_generation_visual_styles_schema',

    # Base schemas
    'success_response_schema',
    'error_response_schema',
]